/** Watcher 观察者，用于发射更新的行为 */

class Watcher{
  /** 
   * @param{Object} vm JGVue 实例
   * @param{String|Function} expOrfn 如果是渲染watcher,传入的就是渲染函数，如果是计算watcher，传入的就是路径表达式，暂时只考虑exOrFn为函数的情况
   * 
   */
    constructor(){

    }
}